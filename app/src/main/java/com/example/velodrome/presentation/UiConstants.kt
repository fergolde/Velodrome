package com.example.velodrome.presentation

import androidx.compose.ui.unit.dp

/**
 * Shared UI constants for the app
 */
object UiConstants {
    /** Margin between MiniPlayer and bottom navigation bar (when outside Scaffold content) */
    val MiniPlayerBottomMargin = 8.dp

    /** Margin when inside Scaffold with bottomBar (accounts for nav bar height) */
    val miniPlayerMargintoNavigationBar = 0.dp

    /** Height of the MiniPlayer */
    val MiniPlayerHeight = 72.dp

    /** Corner radius for MiniPlayer */
    val MiniPlayerCornerRadius = 16.dp

    /** Horizontal padding for MiniPlayer */
    val MiniPlayerHorizontalPadding = 8.dp
}